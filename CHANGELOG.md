# Changelog

> RR-vps 自 6.6.9 起作为新的公开稳定版本基线（RR-vps public stable history starts from 6.6.9）。此前版本仅视为开发阶段，不再作为项目公开正式历史保留。

## 7.1.1 - 安全与发布门禁维护版（2026-08-27）

### 安全修复
- 修复个人订阅路径归一化绕过、Nginx 代理来源伪造、Webhook/远程管理 SSRF、请求体无限读取、远程状态 XSS、恶意 `.rrbak` 注入 root cron/systemd 单元等问题
- 订阅发布根必须是 root 所有的真实目录；PID/状态文件迁至 `/run`，拒绝 `/tmp` 符号链接抢占、非 root 目录和不安全事务备份
- 移除公网明文 HTTP 订阅：独立订阅端点仅允许 `SUB_ACCESS_MODE=https` 下的可信域名 TLS，或默认仅监听 `127.0.0.1` 的本地模式；无有效可信证书时不开放公网端口，需通过 SSH `-L` 转发使用
- 旧版 `http://服务器IP:订阅端口/...` 地址升级后失效且不会重定向；旧地址、token/UUID 及其返回的节点凭据应视为可能已泄露，升级不会自动轮换所有相关凭据，建议按影响范围手动轮换并重新下发订阅
- Nexus 域名面板的个人订阅固定使用 HTTPS `/sub` 路由，Nginx 与后端均不把 token 写入 request-line 日志；公网 IP 自签面板地址不提供公开订阅，只有另有可信订阅域名时才使用独立 HTTPS 入口
- 面板与订阅响应服务限制为 64 个并发请求并设置 15 秒慢客户端超时
- 管理员密码最低长度提高到 12 位；密码、TOTP 或 Passkey 变更后撤销该管理员全部旧 Session 并要求重新登录；API 分别执行 32 KiB/1 MiB 请求体上限
- 公网与本地面板统一使用当前 Origin/标签页 `sessionStorage` 中的 Authorization Bearer，不再接受 Cookie 会话，避免同一域名其他 HTTPS 端口收到管理令牌；旧公网 Cookie 前缀作废并主动清除
- 审计、通知、系统与流量历史加入保留期和硬上限；健康及自动订阅日志限制增长并使用 600 权限
- 更新、备份、恢复、健康检查与 Nexus 同步统一使用 `/run/rr-vps/locks` 下 root 专用锁，拒绝符号链接、硬链接、可写父目录和打开过程替换，避免低权限用户抢占固定锁名阻断维护
- 所有订阅响应（含静态主/恢复/UUID 路由和 304）统一发送 `no-store`，避免轮换前的节点凭据被浏览器或共享代理缓存

### 更新、Release 与供应链
- Stable 只从 CI 完成后生成的 GitHub `latest Release` 读取 `install.sh`、manifest 和 bundle；Beta 才跟随独立 `beta` 分支
- Release 同时发布 `install.sh`、`manifest.sha256`、确定性 bundle、`RELEASE_INFO` 与 `SHA256SUMS`，并拒绝覆盖或修补同名既有资产
- Bootstrap 与核心安装器固定到同版本 Tag；用户镜像只能搬运由官方 manifest/内置摘要校验的内容，不能提供可执行信任锚
- GitHub Actions 使用固定提交摘要与最小 Token 权限；Release 只接受同一 main 提交的完整 CI 成功结果
- RR Nexus 核心构建仅允许在 `main` 上手动或定时运行，并要求目标提交已有成功 CI；审计分支改动不再触发提前发布
- RR Nexus 核心资产固定发布为按版本 Tag 消费的稳定辅助 Release，并显式设置 `latest=false`；消费者核对发布状态、构建来源、上游 Commit 与资产摘要。只有包含完整安装资产的 RR-vps 正式版可成为仓库 Latest，发布后还会核对 Tag 与五项资产，避免 Stable 安装与热更新链接被核心 Release 劫持成 404
- 自建流量统计 Sing-box 改用带构建修订号的隔离 Release（`rr-nexus-core-v…-r1`），严格校验上游 Tag、Commit、两架构摘要与解压后的普通文件/硬链接数；旧版仍可读取原 Tag，候选不再信任测试期生成的旧核心
- 上游 1.13.20 或 1.13.21 尚无对应不可变核心时，7.1.1 仅允许回退到产品代码内固定版本、文件大小与分架构 SHA256 的已审计 1.13.19 核心；白名单由产品常量精确锁定，1.13.22 及未知后续版本仍拒绝，且只有精确目标 Release 返回 404 才启用该一次性例外，网络、权限或元数据异常仍安全拒绝，后续可用的不可变核心仍优先
- bundle 在解压前执行成员数、单文件/总展开大小、尾部填充和双解析器一致性检查，拒绝链接、特殊文件、PAX/GNU 扩展、重复路径、截断与压缩炸弹；用户镜像只有完整 manifest 与可信来源一致时才能参与版本判断

### 正确性与恢复
- 修复 systemd 启动中断恢复时订阅进程随 oneshot 恢复服务退出而被 cgroup 回收的问题；恢复路径现在用独立、可自动回收且不接管应用 PID 文件的瞬态 scope 托管订阅进程，并保留严格 PID/进程身份终态校验
- 修正 Ubuntu 22.04 升级实机审计契约：空订阅域名通过配置白名单解析验证，并在保留手动回滚窗口期间接受“精确旧版放行规则 + 仅回环监听”的安全过渡态，同时从外部验证订阅端口不可达
- 修复旧版订阅隔离单元停止后残留在 systemd failed 缓存、使安全版本虽已升级却无法完成隔离收尾的问题；删除单元后清除失败状态并严格验证 unit 已完全消失，验证失败时继续保留防火墙隔离
- 修复出站域名 DNS 自举闭环、更新在旧运行目录已移动后失败却丢失回滚源，以及设备仅改备注仍可能触发不必要重启的问题
- 补齐 NaiveProxy 首装域名校验函数，并在 Certbot 前安全配置 Nginx ACME Webroot 与 80 端口；兼容 `/var/www` 为 700 的 VPS 模板，只增加父目录可穿越权限，避免合法域名被无限拒绝或证书挑战误报 404
- 修复 Nexus 公网 IP 模式安装完成后调用不存在的地址函数，导致健康检查误报不可达的问题
- 兼容 7.1.0 公网 IP 面板证书的严格 `0600` 权限；热更新仍拒绝非 root、符号链接、多硬链接、异常权限证书及任何非 `0600` 私钥
- 修复安全版本提交并清理旧订阅隔离后未恢复升级前订阅进程的问题；最终服务状态在发布收尾证据前再次校验，失败时保留事务与维护证据且不回滚已提交版本
- 修复 Debian 12 / Ubuntu 22.04 默认 mawk 不支持 SHA256 `{64}` 区间表达式，导致正确 Nexus 流量核心在下载归档前被误拒绝的问题；CI 改用发行版默认 mawk 回归
- 更新快照、回滚、迁移与卸载按命令行和工作目录双重校验订阅 worker，并兼容 `/run`/旧版 `/tmp` PID 状态，避免跨版本回滚后遗留占用订阅端口的孤儿进程
- 孤儿订阅 worker 的回收进一步要求 root 身份、精确 NUL 参数与可信 cwd；只有内核确认 `st_nlink=0` 的真实已删除工作目录才接受 ` (deleted)` 标记，避免误杀同名字面目录或诱饵 Python 进程
- 已提交更新在完成收尾与全局同步后写入持久 `committed-settled` 证据；开机恢复和下一次更新不再重放过期的服务快照，也不会因仅供人工回滚的旧快照损坏而冻结当前安全版本；人工回滚先持久建立事务绑定的维护门禁、再进入 `rolling_back`，收尾或阶段目录同步失败均保留证据并按至少一次语义安全续跑
- 修复首次安装时“所有写入服务均未运行”被误判为快照失败；未运行、已禁用现在作为有效状态被精确记录并恢复
- 安装依赖时保留系统已有 UFW 及其规则，仅在未安装 UFW 时启用 iptables-persistent，避免 apt 静默替换用户防火墙
- 修复受支持系统上的 UFW 规则增删误用 `--force` 而被真实命令拒绝；无终端安装完成后的菜单暂停遇到 EOF 不再把成功安装误报为失败
- 三机发布审计会先把专用测试机的残缺 UFW live chains 重置到已知 inactive 基线，并在启用前后分别核验 SSH 保存规则与实时规则，避免脏测试状态造成误报
- 恢复流程仅接受 RR 白名单数据；Nexus 服务、健康单元和自动更新 worker 均由已校验本机运行时重新生成
- 新增上述安全问题、慢客户端资源上限、历史清理、恶意归档、更新故障阶段与 Ubuntu 22.04 Python 兼容回归测试

## 7.1.0 - 正式版：可恢复热更新、体检迁移、安全告警与 Naive H3（2026-08-27）

### 持久事务式热更新
- 更新流程升级为持久事务状态机：预检、运行快照、准备、原子切换、数据库迁移、健康门禁、提交或回滚均记录在 `/var/lib/rr-update/transactions`
- 写入服务在可恢复事务指针落盘后才会冻结；Nginx、Cloudflared、RR 防火墙规则及服务启停状态纳入版本化外部快照，恢复后再次核验，不完整时保持冻结并保留证据
- 新增开机早期恢复服务和独立 `rr-update-recover`，更新进程被杀、SSH 中断或系统重启后会先识别事务阶段，再恢复旧运行目录、配置、数据库及服务启停状态
- bundle、安装核心和更新守卫分别使用固定 SHA256 校验；运行目录切换前完成完整性、版本、防降级与 Python / JavaScript / Bash 预检
- 更新后检查 Sing-box 配置与进程、Nexus 数据库 quick_check、Nexus `/healthz`、订阅进程和原服务状态；任一门禁失败触发自动回滚尝试，恢复不完整时保留事务证据并要求执行恢复命令
- 保留最近一次已提交事务，支持 `rr update --rollback`；新增 Stable / Beta 通道和 `rr update --check`，发布构建可确定性复现并验证所有固定摘要
- CI 故障注入覆盖旧运行目录已移动、新运行目录已切换和数据库迁移阶段；Debian 12、Ubuntu 22.04、Ubuntu 24.04 容器矩阵执行安装、更新、中断恢复、手动回滚及卸载闭环

### 一键体检、加密备份与跨机迁移
- 新增 `rr doctor`，检查系统、DNS、时间、公网 IPv4/IPv6、Sing-box 配置与端口、防火墙、证书、Argo、Nexus、订阅、数据库、磁盘和更新源
- `rr doctor --repair` 只执行服务重载、目录权限等白名单安全修复；`--report` 输出自动遮蔽 UUID、IP、Token、域名和密钥的报告，`--json` 便于自动化接入
- 新增 `rr backup` / `rr restore`：`.rrbak` 使用 scrypt 派生密钥与 AES-256-GCM 认证加密，内含校验清单、SQLite 一致性备份、配置、设备、额度、流量周期、协议、Reality、Argo、Nexus、自动优选和 RR 定时任务
- 恢复前验证文件成员、摘要、数据库和系统环境；只覆盖 RR 管理范围并保留其他 crontab 项，恢复后健康检查失败会自动还原新服务器原状态

### RR Nexus 安全、告警与管理效率
- 新增 TOTP 两步验证、一次性恢复码登录和 WebAuthn Passkey；严格校验 RP ID、Origin、挑战、签名算法及签名计数器
- 新增 Telegram / HTTPS Webhook 告警中心，覆盖服务离线、磁盘、服务器套餐、证书、设备额度、Argo 临时域名变化、更新/备份失败和安全锁定，支持事件去重、敏感字段遮蔽及 Webhook HMAC 签名
- 新增 CPU / 内存 / 磁盘和上下行 24 小时、7 天、30 天历史趋势；样本自动聚合与清理
- 新增设备分组、配置模板、筛选和批量启停、移组、套模板、重置、删除；修复旧缓存前端选择模板后被默认“0 GB”覆盖的问题
- 将 TOTP / Passkey、通知和备份加密拆入独立库模块；SQLite 连接改为确定提交、回滚与关闭，降低长时间运行的文件描述符占用

### NaiveProxy HTTP/3 与安装完整性
- NaiveProxy 支持 HTTP/2、HTTP/3/QUIC 或双模式，新增 BBR / CUBIC / New Reno 拥塞控制和 `naive+quic://` 分享链接
- 旧配置升级默认保持 HTTP/2，新安装默认双模式；按模式分别开放 TCP / UDP，订阅、二维码、设备配置、Nexus 和远程面板同步识别 H2/H3
- 修复首次安装 Naive 时证书申请顺序、共享端口 TCP/UDP 校验，以及新装多协议未完整写入防火墙规则的问题

### 验证
- 新增 TOTP RFC 向量、WebAuthn ES256、Origin/计数器、CBOR 严格解析、AES-GCM 篡改拒绝与原子输出、Webhook HMAC、7.0.3 数据库无损迁移测试
- 完成 Nexus 登录、分组模板、模板回填、设备创建、批量停用/启用、告警保存、TOTP 初始化和 30 天历史范围浏览器实测

## 7.0.3 - 正式版：个人订阅、周期配额与服务器流量管理（2026-08-26）

### 个人订阅与客户端兼容
- mihomo、Clash Verge Rev、FlClash 拆分为各自独立的个人订阅地址与二维码；旧 Clash 与 Reality 单节点地址继续兼容热更新
- Sing-box 官方入口升级为完整多协议远程配置，选择器与自动测速组补齐 NaiveProxy 和 Argo 优选副节点
- 修复 mihomo YAML 将 TCP Keep Alive 写进代理节点、策略组使用无效 `default` 字段导致节点不通的问题；心跳改为顶层字段，默认节点改用 `default-selected`
- 所有个人订阅统一返回标准 `Subscription-Userinfo`、更新间隔与到期时间；主订阅静态服务升级为 RR 专用响应服务，使本地、公网 IP、公网域名三种模式均可动态显示上传、下载、剩余流量和到期时间
- 九种个人订阅在刷新时动态把“已用｜剩余｜到期”作为首个信息项；NekoBox、v2rayN、v2rayNG、Shadowrocket 与通用 URI 使用明确标注“勿选”的本机回环标记，确保 NekoBox 开启去重后仍保留且不会把流量发往远端，Sing-box 与 Clash 系列使用真实节点副本，源订阅文件保持不变
- 修复热更新只替换订阅服务文件、旧常驻进程却继续返回旧正文的问题；订阅服务状态加入程序哈希，文件变化后自动安全重启，并统一以设备 `used_bytes` 生成响应头和首位信息项
- 删除设备或令牌失效后订阅继续严格返回 404；设备停用、到期或额度用尽时返回带流量信息的空订阅，客户端仍可更新套餐状态但不能继续使用节点

### 设备配额与生命周期
- 个人设备新增自然月自动重置：可设置首次日期与续期次数，重置时保持原额度，月末日期按锚点计算防止逐月漂移
- 自动重置期间额度用尽即停用，到期日自动清零并恢复；最后一次续期结束后按计算出的最终日期到期
- 额度用尽后未发生自动或手动重置的设备，自动删除宽限期由 15 天延长为 35 天
- 设备备注新增本地与远程修改入口；备注仅保存在管理数据库，客户端节点名使用稳定随机别名，修改备注不重启 Sing-box、也不改变订阅地址与节点名

### 服务器套餐与远程管理
- RR Nexus 新增服务器流量套餐：可填写运营商总额度，并可在任意时间按运营商账单校准当前已用量；校准后重新建立网卡基线，再继续统计双向、仅 TX 或仅 RX 流量
- 网卡计数保存基线与累计差值，兼容面板重启、系统重启、网卡计数回退和自动选择默认出口；该功能只负责监控与预警，不会因运营商口径差异自动停止整台 VPS
- 远程副面板同步支持服务器套餐、设备周期重置、设备备注与完整个人订阅管理
- 本地面板 SSH 隧道加入 30 秒主动保活、6 次容错及端口转发失败快速退出，降低 NAT 或家宽长时间空闲造成的掉线概率

### 验证与发布
- 兼容旧数据库无损迁移；覆盖自然月末计算、自动重置恢复、35 天清理、真实网卡累计、重启与计数回退、动态订阅头和失效令牌拒绝测试
- 21 个发布清单文件、22 个 bundle 成员、6 种节点 URI、9 种个人订阅入口、前端契约与 Linux GitHub Actions 纳入统一回归

## 7.0.2 - 正式版：浏览器 Edge 初筛与更新链路加固（2026-08-24）

### 新增
- RR Nexus 新增 Cloudflare Edge 浏览器候选初筛：内置 1000 域名池，分层筛选 Stage 1 → Stage 2 → TOP 20
- 移动网络严格以 `openai.com` 为质量基准；Wi-Fi / 宽带使用 `openai.com`、`deepl.com`、`cloudflare.com` 三项参考门禁
- 支持均衡模式、亚洲入口狩猎、IPv4 / IPv6 / 双栈分组、成功率、TTFB、轮次波动、POP 与本机历史记录
- 面板初筛全部在用户浏览器执行，RR-vps 服务器不参与测速计算，也不接收结果

### 产品定位
- **面板优选质量不能只看排名**：排名只是浏览器候选顺序，不代表真实代理质量，也不保证第一名或前三名就是实际最快入口
- 用户必须把 TOP 20 放进真实客户端逐个测试，最终以实际速度、稳定性和晚高峰表现为准
- 面板版服务于没有原生测试环境的用户；Android **CF 域名优选 2.7.1** 可用于移动、电信、联通三网候选筛选，结果仍须在当前运营商、时段和真实客户端中复测
- Android 2.7.1 本版未改动

### 修复与加固
- 修复 Stage 1 成功轮次未计入，三轮全成功候选被错误显示为 67% 的问题
- 亚洲入口狩猎从仅有界面选项改为真实配额筛选与 HKG > NRT > SIN > ICN > TPE 排序
- 失败轮次按 0 纳入波动；VPN、出口变化或基准门禁失败时不保存为历史最佳
- 移动网络、宽带、运营商和协议的历史结果相互隔离，避免跨环境污染
- 更新器增加用户镜像 → GitHub Raw → GitHub 官方 Contents API → IPv4 CDN 多源回退，并区分下载链路失败与远程清单无效
- CI 正式执行仓库完整回归套件，覆盖 Bash 语法、发布清单、SHA256、Nexus 与更新链路契约
- 脚本终端为 VMess、VLESS Reality、Hysteria2、TUIC、AnyTLS、NaiveProxy 全部节点补齐二维码，并统一使用 M 级纠错与标准静区
- RR Nexus 修复公网 IP、IPv6、NAT/LXD 映射端口的订阅二维码地址；只展示真实生成的订阅格式，缺失格式不再产生无效二维码或错误回退
- 本地与远程面板二维码统一校验目标 URL、PNG 输出和异常超时，节点与订阅二维码使用同一条受检生成链

### 验证
- 1000 域名浏览器完整流程、移动网络 OpenAI 单基准、宽带三基准和 390px 手机布局通过
- 20 个发布清单文件、21 个 bundle 成员、6 种节点 URI、8 种面板订阅格式、前端单元检查与 Linux GitHub Actions 全部通过

## 7.0.1 - 正式版：Nexus 面板修复与远程设备管理闭环

### 修复
- 设备在线统计：新增「实时在线」口径（近 5 分钟有流量且启用），卡片显示「X 实时在线 · Y 启用」
- 防火墙端口状态：节点协议已监听端口即显示开启（原仅看防火墙规则，节点无独立规则时误报关闭）
- 分流下拉框白字：深色背景适配
- 分流应用后即时刷新状态；右上角刷新按钮覆盖防火墙/服务器/多服务器视图
- GPT 解锁检测：改用 cdn-cgi/trace 出口判定，数据中心 IP 不再误判地区封锁
- Cloudflare 隧道状态：快速隧道模式增加进程检测兜底
- CPU 参数固定宽度，大数字不再挤压布局
- 服务器卡片误显示「未同步」：修复状态合并逻辑
- 远程按钮失效与进度条满死：CSP 内联样式拦截修复（data-w + CSSOM）
- 远程设备 enabled 判断兼容 JSON 布尔
- 远程设备卡片对齐本地：绿灯状态 + 二维码透传
- 设备操作（添加/重置/修改/删除）同步后台化：节点重启不再误报断连
- 设备重名校验 + 错误提示中文化

## 6.6.18 - 订阅二维码改「打开二维码」按钮（与单节点一致）

### 调整
- 订阅地址列表二维码交互对齐单节点：每项右侧「打开二维码」按钮，
  点击在新窗口打开大图（window.open），扫码更稳
- 移除上一版点击放大弹窗方案（用户反馈按钮方式更方便）

## 6.6.17 - 订阅二维码支持点击放大

### 新增
- 订阅地址列表每项的二维码支持点击放大：弹出居中大图（320px 锐利渲染），
  方便扫描添加设备；点击任意位置关闭
- 前端 modal 纯 JS 动态创建，不新增依赖

## 6.6.16 - 面板订阅地址列表每项都带二维码

### 新增
- 设备详情页的订阅地址列表（总订阅/NekoBox/URI 全集/Sing-box 完整）此前只有
  第一项有二维码；现在每项右侧都有二维码，扫码即得订阅，方便快速添加设备
- 后端 /api/devices/{id}/qr 新增 raw 参数（仅接受 http/https），前端按 URL 逐项生成

## 6.6.15 - 修复 LE 证书 403（root umask 077 权限链）

### 修复
- DMIT 等模板 root umask 077：mkdir 出 700 目录、certbot 写 600 文件，
  nginx(www-data) 读挑战文件 Permission denied → LE 验证 403（五哥实机复现）
- 面板域名模式与 NaiveProxy 的 webroot 流程：显式 chmod 755 目录链
  （webroot/.well-known/acme-challenge）+ umask 022 子壳跑 certbot（文件 644）
- 实机对比：Vultr（umask 022）一次过，DMIT（umask 077）403——现已强制权限

## 6.6.14 - 修复面板域名模式 LE 证书 404（nginx 未 reload）

### 修复
- nexus_write_nginx_site / nexus_write_nginx_custom_port 写完 server 块只做 nginx -t
  不 reload——nginx 继续跑 default 站，acme-challenge 请求 404，Let's Encrypt 签发失败
- 现在语法检查通过后立即 reload（reload 失败退级 restart），新配置即时生效

## 6.6.13 - 修复面板安装 typing_extensions 冲突（6.6.10 回归）

### 修复
- 6.6.10 起 grpcio 统一 pip 安装，在 Ubuntu 24.04 上 pip 升级 apt 版 typing_extensions
  时报 "Cannot uninstall typing_extensions 4.10.0, RECORD file not found" 导致面板装不上
- 新策略：已装新版跳过 → apt 源版优先（Debian12=1.51/Ubuntu24=1.60，无 pip 冲突）
  → 仅 Ubuntu22（源版 1.41）走 pip 兜底，兜底前先 --ignore-installed 装 pip 版
  typing_extensions shadow 掉 debian 版（/usr/local/lib 优先）
- 实机验证：CS2 Ubuntu22 卸净重装走 pip 兜底 1.83 面板 active；CS3 半删残留异常态
  亦兜底成功；面板服务不受影响

## 6.6.12 - 订阅服务自愈链加固

### 修复
- 健康检查自愈订阅时 `generate_node_and_sub` 无超时保护：重建含公网入口探测，网络抖动会把整个健康检查链卡住，导致订阅服务迟迟未拉起。现在加 `timeout 90` 兜底，卡住也不阻断后续检查

### 验证
- CS1 实测：杀订阅服务 → `rr --health-check` 一次拉起（3.8s）；timer 每 5 分钟自动巡检正常
- 前链实测（6.6.11）：H1 截断 bundle 回滚 / H2 乱码 manifest 拒绝 / B5 default→singbox_node 全过

## 6.6.11 - P2 修复：防火墙关闭拒绝基线 + 设备同步 O(n) 合并

### 修复（P2 ×2）
- **防火墙 close 无拒绝基线**：INPUT 默认策略 ACCEPT 时，删除放行规则后端口依然可达。现在 `close_protocol_firewall` 在链尾补一条 `DROP`（comment: argo-rr-managed-block），关闭=明确拒绝；`open_protocol_firewall` 先删该 DROP 再放行，语义闭环（iptables/ip6tables/ufw 三路一致，不影响其他服务）
- **设备操作 O(n)**：批量设备变更（如批量删除）触发 N 次全量重建（每次 13-53s），面板被拖到 504。`sync_devices` 加合并窗口：同步进行中时新请求标记 pending 秒回，执行完 drain 兜住期间全部变更——批量 20 并发实测从 20 次重建合并为 2 次

### 验证
- 防火墙（CS1 实机）：open=HTTP 200 连通 → close=DROP 在位+连接超时拒绝 → re-open=HTTP 200 恢复；规则无残留
- debounce（本地并发单测）：20 并发调用全部 True、实际执行 2 次（首+尾 drain）
- 面板 smoke：CS1 加载新 rr_nexus.py 重启后 active、HTTP 200

## 6.6.10 - P1 修复：grpcio 旧版面板 CPU 死循环

### 修复（P1）
- grpcio < 1.43 在 Ubuntu 22.04 上会导致 Nexus 面板 CPU 死循环（apt 源 python3-grpcio 为 1.41，过旧）
- `nexus_install_dependencies` 改用 pip 统一安装 `grpcio>=1.43`（兼容 PEP 668：Debian 12 / Ubuntu 24.04 自动加 `--break-system-packages`），并卸载 apt 版避免并存冲突，装后强制校验版本
- 幂等：已装新版时 pip 自带版本比较直接跳过；更新流程面板重启后自动加载新版

### 验证
- 三系统实机（Debian 12 / Ubuntu 22.04 / Ubuntu 24.04）：依赖函数 FN_RC=0、grpcio 1.83.0、面板重启后 3 分钟观察 CPU 0.2%（无死循环）

## 6.6.9 - 稳定基线（系统级审计修复版，全量实机验证 103 PASS/0 FAIL）

### 修复（7 个产品缺陷全闭环）
- 到期设备不清扫 + 订阅服务与面板 /sub 双路由语义不一致 → 幂等清扫标记 + SQL 过滤对齐
- toggle 失败伪装成功（403 变 200 {}）→ 非零退出也解析 stdout 透传真实状态
- 无公网证书时 revoke 会重新生成 key → 与签发对称补证书门槛
- Nexus 数据库损坏无限崩溃循环 → quick_check 完整性检查 + 退出码 3 + StartLimit（不自动重建，数据安全优先）
- Naive LE 证书邮箱 admin@example.com 被 LE 恒拒 → 未配置时从域名派生，LE_EMAIL 可配置
- /nexus/ 目录泄露设备 token → index.html 防护
- cron worker PATH 缺 /usr/local/bin → 孤儿清理失效修复

### 增强（完善项 + 可靠性改进）
- TLS 证书删除后自动重建，节点不再因证书丢失无法启动
- NAIVE 未启用时清空遗留掩码凭据，保持配置终态干净
- 迁移后活节点机器健康定时器介入，判定收紧
- 直连自签模式补防火墙规则
- SNI map 修复（443 节点流量精确归属面板）
- 审计补全两路径（成败双落账 + revoke denied）
- 断网时更新检查三态（有新/已最新/检查失败），不再误报"已是最新"
- 内核自愈失败写日志 + 面板提示（不再静默吞错）
- 孤立 sing-box 进程三通道提示（仅提示不清理）
- 降级拦截显式提示
- 升级失败自动回滚全链路（坏 bundle/坏 config/降级不兼容）
- 旧版配置迁移后热更升级字段零丢失（兼容性实测通过）
- Debian 12 / Ubuntu 22.04 / Ubuntu 24.04 全新安装实测通过
