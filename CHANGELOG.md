# Changelog

> RR-vps 自 6.6.9 起作为新的公开稳定版本基线（RR-vps public stable history starts from 6.6.9）。此前版本仅视为开发阶段，不再作为项目公开正式历史保留。

## 7.2.0 - 正式版：安全边界重构、事务恢复加固与可信发布（2026-09-01）

7.2.0 是 RR-vps 在 7.1.0 功能基线之上的一次大规模工程升级。本轮重点不是简单增加菜单项，而是系统重构订阅暴露、RR Nexus 会话、备份恢复、热更新事务、跨版本回滚、二进制供应链和正式发布链路，使高权限安装、更新、恢复与卸载过程具备更明确的信任边界、持久证据和失败收敛能力。启用 RR Nexus 实时流量统计时使用的服务端核心正式固定到 sing-box 1.14.0。

相较 v7.1.0，本轮仓库级变更覆盖 80 余个文件路径，新增与重构 7 万余行实现、测试和发布审计逻辑；正式变更范围以 `v7.1.0..v7.2.0` Tag 差异为准。

### 升级前必读
- 公网明文 HTTP 订阅已经移除。独立订阅端点只允许使用可信域名证书的 HTTPS，或默认仅监听 `127.0.0.1` 并通过 SSH 端口转发访问；无域名用户可在 Nexus 选择 Let's Encrypt 短期 IP 证书模式，通过可信公网 IPv4/IPv6 HTTPS 使用个人订阅
- 公网 IP 可信模式要求该地址全球可路由且由本机实际提供服务，公网 TCP/80 必须持续到达 RR 的 ACME Webroot，客户端和本机必须能通过系统 CA 验证精确 IP SAN；私网、保留、文档、CGNAT 地址或受阻的 80 端口均会在联网或发布前拒绝，且不会回退到自签订阅
- 旧版 `http://服务器IP:订阅端口/...` 地址升级后会失效且不会自动重定向；旧订阅地址、Token、UUID 及其返回的节点凭据应视实际暴露情况决定是否轮换
- 直接运行新安装命令或使用 `rr update` 即可从 7.1.0 升级，无需预先卸载；现有节点配置、RR Nexus 数据库、设备、流量状态及受支持的证书状态继续纳入迁移与恢复
- 启用 NaiveProxy 的 `.rrbak` 不携带 Certbot 账户和续签配置：目标必须是已安装当前 RR-vps 的现有实例，并预置同域名、可续签的 Certbot lineage 与当前部署钩子；恢复会在写入前验证证书信任、域名、私钥及至少 7 天剩余有效期，空白或未准备目标会被安全拒绝。未启用 NaiveProxy 的备份仍可直接恢复到空白目标
- 活动 UFW 仅在用户规则为简单格式且可证明与 RR 端口互不重叠时接受便携恢复；同端口规则或无法安全证明顺序独立的复杂规则会在任何防火墙写入前拒绝恢复
- 为避免旧版订阅服务在安全回滚后重新暴露，回滚到 7.1.1 之前的运行时可能进入明确的“隔离/降级”状态。这是安全拒绝行为，不代表当前安全版本升级失败

### 订阅、会话与管理面安全基线
- 修复个人订阅路径归一化绕过、Nginx 代理来源伪造、Webhook 与远程管理 SSRF、远程状态 XSS、无限请求体读取等信任边界问题
- 订阅发布根必须是 root 所有的真实目录；PID 与运行状态迁至 `/run`，拒绝 `/tmp` 符号链接抢占、非 root 目录、多硬链接及不安全事务备份
- Nexus 域名面板的个人订阅统一使用 HTTPS `/sub` 路由；Nginx 与后端不再将订阅 Token 写入 request-line 日志
- 历史公网 IP 自签面板不再隐式提供公开订阅，也不能签发远程钥匙；只有可信订阅域名，或已完成 `acme-ip-shortlived` 证书发布和系统 CA 验证的全球公网 IP，才能生成公开 HTTPS 个人订阅
- 所有订阅响应，包括静态入口、恢复入口、UUID 路由及 `304 Not Modified`，统一发送 `Cache-Control: no-store`，降低轮换前凭据被浏览器或共享代理缓存的风险
- 公网与本地面板统一使用当前 Origin、当前标签页 `sessionStorage` 中的 Authorization Bearer，不再接受 Cookie 会话，避免同一主机名下其他 HTTPS 端口收到管理令牌
- 管理员密码最低长度提高到 12 位；密码、TOTP 或 Passkey 发生变更后撤销该管理员全部旧 Session，并要求重新登录
- API 按用途执行 32 KiB 或 1 MiB 请求体上限；面板和订阅响应服务限制为 64 个并发请求，并对慢客户端设置 15 秒超时
- 审计、通知、系统状态和流量历史加入保留期及硬上限；健康检查与自动订阅日志限制增长并统一使用严格权限
- 更新、备份、恢复、健康检查、Nexus 同步、交互菜单、CLI 写入口及证书部署钩子统一进入 `/run/rr-vps/locks` 下的 root 专用事务锁域；受信委托同时校验进程祖先、锁 inode、FD 所有权和旧版兼容锁，忙碌或伪造上下文安全拒绝，纯读操作仍可使用

### 持久更新、回滚与开机恢复
- 更新流程进一步固化为持久事务：运行目录、配置、数据库、服务状态及外部系统状态在切换前形成可验证快照，失败后按事务阶段恢复
- 新增并严格校验 `committed-settled` 持久终态证据；已提交且完成收尾的事务不会在下次启动或更新时错误重放旧服务快照
- 人工回滚会先持久建立与事务绑定的维护门禁，再进入 `rolling_back`；阶段目录同步或收尾失败时保留证据，并按至少一次语义继续安全收敛
- 回滚到 7.1.1 之前的运行时会建立订阅隔离单元和精确防火墙屏障，只阻止可能重新暴露的旧订阅服务，不清除用户其他防火墙规则或备份数据
- 隔离规则按照内核实际 IPv6 启用状态分别核验 IPv4/IPv6 后端；卸载和再次升级后会等待隔离文件、systemd 状态、守护进程及精确防火墙规则全部收敛
- 修复旧隔离单元停止后残留在 systemd failed 缓存、导致安全版本已经升级却无法完成隔离收尾的问题；验证失败时继续保留安全屏障
- 修复开机恢复服务退出时，订阅进程被 oneshot cgroup 一并回收的问题；恢复路径改用独立、可自动回收且不接管应用 PID 文件的瞬态 scope 托管进程
- 更新提交并清理旧订阅隔离后，会按升级前记录恢复订阅服务，并在发布收尾证据写入前再次校验最终状态
- 更新快照、迁移、回滚与卸载均通过命令行、进程身份、root 所有权和可信工作目录联合识别订阅 worker；只有内核确认已删除的真实旧目录才允许回收孤儿进程
- 修复更新过程中旧运行目录已移动后失败却丢失回滚源，以及出站域名解析形成 DNS 自举闭环的问题
- 首次安装时“所有受管写入服务均未运行”现在会被记录为有效快照状态，不再误判为事务失败
- 实机审计预清理在新运行时上最多等待 180 秒取得完整更新锁，并兼容旧版无统一锁的卸载入口；非锁竞争错误继续立即失败，不会被重试掩盖
- 直接安装与热更新在依次取得统一更新锁和旧版兼容锁后，先检查 `/var/lib/rr-backup/active`；只要该路径存在任何对象，包括常规文件、目录、有效或悬空符号链接，都会在发布下载和运行时准备之前安全拒绝。安装器不解释、不修复也不删除恢复证据
- 所有防火墙写入口在首个 UFW、iptables/ip6tables、NAT 或持久化写入前，先安装并证明独立 systemd supervisor 与组合门禁，持久发布 `inflight` 日志并同步停用受管公网入口；只有 live 与持久规则均精确复核后才清除日志并恢复原态。进程在首写、保存、补偿或清理任一边界被 SIGKILL 时，开机门禁仍会阻止入口启动，并将不确定状态收敛为持久隔离
- Restore、持久防火墙隔离与 Nexus IP 证书门禁可以按受管顺序同时存在；不再用空 `ExecCondition=` 擦除管理员或上游策略，遇到未知条件、忽略错误前缀、后序 reset 或无法证明的有效属性会在服务变更前拒绝

### 备份、恢复与跨机迁移
- `.rrbak` 恢复改为严格白名单模型：归档成员、路径、类型、数量、单文件大小、总展开大小、摘要和数据库一致性均在写入系统前验证
- 拒绝路径穿越、符号链接、硬链接、特殊文件、恶意元数据及可借恢复过程注入 root cron/systemd 单元的归档
- 恢复只接受 RR 管理数据；Nexus 服务、健康单元、订阅 worker 和自动更新任务均由当前已验证运行时重新生成，不信任备份内的可执行服务定义
- 恢复过程加入持久阶段、看门狗和开机续跑能力；进程中断或系统重启后可识别事务状态，避免留下半恢复配置
- 恢复失败会尝试还原目标服务器恢复前的 RR 状态并保留事务证据；若二次恢复无法通过验证，目标保持隔离并进入 `recovery_failed`，供继续恢复或人工诊断
- 恢复在首次读取目标服务状态前、冻结写入者后、回滚快照完成后以及每次递归清理前重复验证目标形态与挂载拓扑；任何挂载或绑定挂载跨入受管递归删除根，或替换精确写入目标，均在破坏性操作前拒绝
- 配置、服务与恢复单元、密钥、数据库及全部受管 systemd drop-in 树必须为 root 所有、不可由组或其他用户写入，并且只能包含可复现的真实常规文件或目录；常规文件同时拒绝多硬链接。Nginx 只保留 RR 三个精确的 `sites-enabled` 到 `sites-available` 符号链接例外
- 完整回滚会先重建受管目录（包括空目录），再按由深到浅的顺序恢复已经验证的安全目录权限；特殊权限位及组或其他用户可写模式不会被重放
- READY 门禁只在候选或原状态达到可启动阶段时开放；服务恢复、后期防火墙或状态验证、终态发布失败时，会先撤销 READY，再停止并核验 Nginx 与受管运行服务。若无法证明已经隔离，事务保留 `recovery_failed` 及诊断证据
- READY 的撤销本身也属于失败关闭边界：若 marker unlink/fsync 无法证明，恢复器会先发布并验证独立防火墙隔离，再停止运行时；保留的 ACTIVE/READY 证据因此不能被晚到的手工重启绕过
- Cloudflared 回滚所有权不再依赖可能已经清空或回放失败的当前配置，而由 root 专用事务快照中的严格服务证据决定；原树应用失败、启动返回失败但进程仍活动、READY 后期验证失败等分支都会再次停止并证明隧道已隔离，同时不会误停没有 RR 所有权证据的第三方 Cloudflared
- NaiveProxy 便携恢复在目标变更前同时验证备份证书、目标同域名可续签 lineage 和当前部署钩子；候选事务只读检查全局 ACME 状态，不调用签发或续签，也不改写钩子，避免产生快照范围外的证书副作用
- 未启用 NaiveProxy 的备份支持空白目标，并将订阅访问面保持为仅回环；已有目标机的独立 HTTPS 订阅访问面会保留，但其可信证书与当前部署钩子也必须通过恢复前检查
- 跨机恢复不再沿用目标机过期的 RR 防火墙集合：旧 RR 规则会被精确清除；通过安全预检的非 RR 用户规则保持不变，再按导入配置重建并持久化期望规则。活动 UFW 若存在同端口或无法证明顺序独立的复杂用户规则，会在防火墙写入前拒绝；后续迁移继续执行零写入只读校验，任一写入、跳跃规则或持久化失败都会触发目标机快照回滚，二次恢复无法验证时保持隔离并保留 `recovery_failed` 证据
- 信号中断、同进程补偿和非 Naive 空目标恢复显式隔离内部迁移模式与事务环境，避免回滚路径误用便携证书分支，让原目标服务状态和防火墙快照继续由同一恢复状态机收敛
- 跨机迁移验证覆盖加密备份传输、数据库稳定指纹、设备流量计数、私有/公开订阅生成树、文件权限、防火墙状态及恢复后 Nexus 健康

### RR Nexus、证书与运行正确性
- RR Nexus 新增无需域名的可信公网 IP 模式：对规范化的全球公网 IPv4/IPv6 使用 lego 5.4.0 和 Let's Encrypt `shortlived` profile，通过 HTTP-01 Webroot 签发短期 IP 证书；IPv6 URL 自动使用方括号。私网、保留、文档和非全局地址在任何 CA 请求前拒绝
- IP ACME 账户、证书候选、活动存储、发布日志和 live cert/key 分层保存；证书候选通过精确 IP SAN、私钥、完整链、系统 CA 与剩余有效期验证后，先原子提交活动存储，再在持久 pending 门禁下发布 cert/key。CA 失败、ENOSPC、双 rename 中断或 SIGKILL 不覆盖健康 live 证书，重启后使用已有候选/日志正向恢复而不重复签发
- IP ACME 使用 root、`UMask=0077` 的静态 oneshot service 和每 12 小时持久 timer；80 端口只暴露受管 `/.well-known/acme-challenge/`，外来 Webroot、站点、符号链接、挂载点、状态树或 systemd 身份一律拒绝采用。续签和恢复进入统一写锁，卸载只移除摘要与所有权均可证明的 RR 材料
- 修复 7.1.0 公网 IP 面板证书使用严格 `0600` 权限时被升级流程错误拒绝的问题；非 root、符号链接、多硬链接、异常权限证书及非 `0600` 私钥仍会安全拒绝
- 修复 Nexus 公网 IP 模式安装完成后调用不存在的地址函数，导致健康检查误报不可达的问题
- Nexus 服务重启后采用有界本地就绪等待，避免 `Type=simple` 启动延迟被误判为更新失败并触发错误回滚
- 修复设备仅修改备注时仍可能触发不必要 Sing-box 重启的问题
- 修复 Debian 12 与 Ubuntu 22.04 默认 `mawk` 不支持 SHA256 `{64}` 区间表达式，导致正确 Nexus 流量核心在下载前被误拒绝的问题
- NaiveProxy 首装补齐域名校验，并在 Certbot 前安全建立 Nginx ACME Webroot 与 80 端口路由；兼容 `/var/www` 权限较严的 VPS 模板，只增加完成挑战所需的目录穿越权限
- NaiveProxy、独立 HTTPS 订阅与 Nexus 域名面板在签发后验收、既有证书复用、更新、启动及便携恢复时统一执行 Certbot Webroot lineage 的密码学与账户级证明：真实解析叶证书、链、完整链和私钥，校验精确 SAN、逐级签发关系、私钥后端一致性、`live`/`archive` 代次及 `reuse-key`，并验证生产 ACME 注册、账户元数据、RSA JWK、SPKI 派生账户 ID、严格秘密权限及受限 ARI 字段；仅手工复制或形状相似的伪证书、伪账户不足以通过门禁
- “可自动续签”现在同时证明 Certbot service/timer 来自受信发行版单元、无未知 drop-in 或 namespace/sandbox 阻断、以 root 执行精确 `certbot renew`，并验证可重复的每日日历及随机延迟、精度和 offset 后最坏间隔不超过 7 天。Nginx 80 端口除静态 Host/Webroot 结构外，还会在安全目录以 `dirfd/openat`、`O_NOFOLLOW` 建立高熵 challenge，分别经 `127.0.0.1` 与 `[::1]` 携带精确 Host 请求并要求 HTTP 200 与正文逐字匹配；不遮蔽路由的发行版默认站点可以共存，公网 DNS/80 可达性继续由实机 Certbot staging `renew --dry-run` 证明
- 通用证书部署钩子在同一更新/恢复锁内独立解析并处理 Naive、订阅和 Nexus 三类消费者，一个配置损坏不会跳过其他可证明消费者；每次重载前先持久写入绑定 lineage 代次与 cert/key/fullchain 摘要的 root 专用 pending marker，只有本机 TLS 叶证书与当前 lineage 一致才清除。重载失败、SIGKILL 或下次健康检查时会按消费者幂等补偿，篡改 marker 安全拒绝
- Sing-box 的 Naive 证书/私钥双 rename 由持久 pair-pending 门禁保护；Reality private/public/short-id 作为同一原子配置世代发布并从私钥重新派生公钥。服务启动前还会证明精确 ExecStart/ExecReload、运行身份、工作目录、全部允许 drop-in、`ExecCondition` 顺序及 `ignore_errors=no`，重启成功必须观察到新的 MainPID 与 InvocationID
- Nexus 公网 IP 证书使用持久 pending marker 与独立 Nginx 门禁，配置、service 和密钥均采用同目录原子发布并复核有效 systemd 属性；只有短期证书精确匹配 IP、私钥和系统 CA 链，且后端 active+healthy 与公网代理健康全部通过，才开放同源 `/sub/...` 个人订阅。80/面板端口按本次创建、既有规则和共享 ACME 消费者分别记账，公网启用、public→local、失败补偿及卸载只撤销 RR 确认拥有的状态
- 安装依赖时保留系统已有 UFW 及用户规则；仅在没有 UFW 时启用 `iptables-persistent`
- 修复受支持系统上 UFW 规则增删误用参数而被真实命令拒绝的问题；无终端安装结束后遇到 EOF 不再把成功安装误报为失败
- 普通安装、协议菜单、全开/全关、主端口与 HY2/TUIC 跳跃端口统一进入配置+防火墙批事务：跨进程 root-safe 可重入锁覆盖快照、写入、验证、持久化和反向补偿全窗口，操作日志按 LIFO 恢复配置、服务与 UFW/IPv4/IPv6 状态。NAT 不再只证明目标规则存在，而会在首写前及提交后验证 PREROUTING 的有效首匹配，拒绝更早的同端口、范围、multiport、跳链、未知或取反冲突；raw/NAT 缺少受支持持久化后端时在零写入阶段失败，UFW 保存程序与编译后 live chain 的顺序和多重性均需精确复核
- 无法精确证明防火墙补偿时，会保留 root 专用证据和独立 service/path/timer，停止并禁用全部受管入口且跨重启重试；解除隔离必须用新鲜证据重建、持久化并复核完整 RR IPv4/IPv6 filter 与 NAT 命名空间，卸载也会逐项证明并撤销门禁、守护材料和精确规则，不触碰用户其他防火墙状态

### Release、更新源与供应链
- Stable 更新只消费 CI 验证后生成的正式 GitHub Release，并固定到匹配版本 Tag；Beta 继续使用独立分支，不参与 Stable 版本判断
- 正式 Release 固定发布 `install.sh`、`manifest.sha256`、确定性 `rr-bundle.tar.gz`、`RELEASE_INFO` 和 `SHA256SUMS` 五项资产
- 发布流程拒绝覆盖、替换或事后修补同名既有资产，并要求仓库启用 Immutable Releases
- Release 只接受当前 `main` 顶端的完整提交 SHA，且该 SHA 必须分别存在 `ci.yml` 的 `push`、`vps-audit.yml` 的 `push` 和 `vps-audit.yml` 的 `workflow_dispatch` 三类成功证据；验证期间主分支发生变化会立即终止发布
- GitHub Actions 第三方步骤固定到完整提交摘要，并按任务拆分最小 Token 权限；持有写权限的发布阶段不会执行候选仓库脚本
- RR Nexus 统计核心升级到 sing-box 1.14.0，使用 Go 1.25.5、`CGO_ENABLED=0`、官方完整默认构建标签与额外 `with_v2ray_api`，并固定上游不可变 Release、Tag 与源码 Commit；运行时同时验证 Version、Revision、架构、Go、CGO、完整标签和 VCS build info。两种架构资产继续通过带构建修订号的隔离辅助 Release 发布并显式设置 `latest=false`
- 只有包含完整安装资产的 RR-vps 正式版可以成为仓库 Latest，避免辅助核心 Release 劫持 Stable 安装或热更新地址
- 删除 1.13.19 及 1.13.20/1.13.21 回退路径；版本低于、版本高于但身份伪造、源码 Revision/Go/CGO/标签不一致的核心均不能冒充 1.14.0 正式基线，并触发受检事务升级或安全拒绝
- bundle 在解压前执行成员数量、单文件与总展开大小、尾部填充及双解析器一致性检查，拒绝链接、特殊文件、PAX/GNU 扩展、重复路径、截断和压缩炸弹
- 用户自定义镜像仅作为传输源；只有完整 manifest 与可信发布源一致时才可参与版本判断，不能提供版本号或可执行信任锚
- Stable 更新器在执行 bootstrap 前会从官方 API 证明 canonical 版本、不可变正式 Release、精确五资产、唯一 `rr-vps-release-owner:v2` Tag/Body 所有权标记、bot author/uploader、Tag→Commit、`RELEASE_INFO`、`SHA256SUMS` 与资产 API digest 全部一致；下载前后均完整分页并本地重筛同一 SHA 的 CI push、VPS push、VPS workflow_dispatch 三类 run，按 `run_number + run_attempt` 要求最新尝试为 `completed/success`，重复、缺页、`total_count` 漂移、缺事件或旧成功掩盖新失败全部安全拒绝。Beta 才继续使用隔离分支信任
- 产品与 Nexus 核心发布事务使用带 payload 摘要的 annotated Tag、服务端 `force-with-lease` CAS、精确 Draft ID 和逐资产重采纳；网络响应丢失或 1/5 部分上传可在同一 ID 安全续传，未知、外来、公开或不可变状态永不覆盖/删除。辅助核心归档改为确定性 USTAR、固定 mtime 与无 gzip 时间戳，并拒绝与安装器 schema 不一致的路径

### 验证与发布证据
- 自动化门禁包含 ShellCheck、Bash/Python 语法、Manifest 全量 SHA256、确定性 bundle 复现、仓库综合验证及 40 余个独立安全、事务、并发、归档、防火墙、证书、Nexus、供应链和 Release 回归脚本；sing-box 1.14、IP ACME、IPv4/IPv6 订阅 URL、回滚门禁及 Release 三事件证据使用专用 HOME/TMPDIR/XDG 隔离执行，普通夹具保持非 root，只有必须证明 uid 0 所有权的 IP ACME 与门禁回放夹具才使用 sudo，且全部真实系统调用均被 mock 或重定向到临时根
- 容器矩阵覆盖 Debian 12、Ubuntu 22.04、Ubuntu 24.04 的干净安装、热更新、手动回滚和完整卸载闭环，并使用发行版默认 `mawk`
- 三台独立实机发布审计分别覆盖：Debian 12 全协议与公网 HTTPS/UFW；Ubuntu 22.04 从 7.1.0 升级、隔离回滚、再次升级及旧版卸载；Ubuntu 24.04 干净事务、开机恢复、已提交回滚和本地 SSH 隧道
- 独立 `workflow_dispatch` 实机门禁在固定主机密钥校验下，分别为全球公网 IPv4 与 IPv6 执行真实短期 IP 证书签发、12 小时续签 timer、外部系统 CA HTTPS/订阅验证、故障恢复和完整卸载；此证据必须与三机 `push` 审计及 CI 指向同一提交 SHA
- Debian 12 实机对独立的 Nexus 与 Naive 两条生产 lineage 额外执行 Certbot staging `renew --dry-run`，在静态结构、本机路由和防火墙证明之外实际验证 DNS、公网 80 端口与 ACME Webroot 挑战；详细 Certbot 输出仅保留在测试机 root 日志，不进入 Actions 日志
- 跨机实机审计在 A 机依次覆盖 1、10、100、500 台设备规模的数据库同步与订阅生成，并使用最终 500 台设备负载执行加密 A→C 迁移和恢复一致性验证；NaiveProxy 在生成便携负载前临时关闭
- NaiveProxy 跨机路径未纳入本轮 A→C 实机恢复；其备份证书与目标 lineage 信任、空白/未准备目标拒绝、部署钩子只读及无副作用边界由证书、备份预检和事务回归覆盖
- 跨机审计同时验证目标机旧 RR 防火墙规则已移除、非 RR 用户规则逐条保留、导入配置的 filter/NAT 规则完整且只读复核零变更；systemd oneshot 失败缓存会在停机后归一化，再执行严格三元状态断言
- A、C 两机在接收口令、备份和指纹前，均先在 `/var/lib/rr-audit-cross-cleanup-{a,c}` 写入 root 专用持久状态包，并安装、校验 `/etc/systemd/system` 下的 service/timer：75 分钟绝对截止、`Persistent=true`、每 5 分钟重触发、`Restart=on-failure` 且无启动频率上限；helper 摘要、有效 `ExecStart`、timer/service 触发关系和固定绝对路径必须全部匹配，清理失败会跨重启继续重试
- A 侧 helper 会先原子恢复并复核源配置，再恢复 Nexus 与健康计时器，最后删除口令、备份和指纹；C 侧会取得产品防火墙锁，精确移除并持久化 IPv4/IPv6 审计 fixture 与 sentinel，复核 live/persist 均不存在后再删除敏感材料。正常退出、TTL 与下次 preclean 使用同一幂等收敛逻辑，只有全部固定路径都确认不存在后才撤销自身 unit
- 实机连接固定校验独立保存的 Ed25519 主机密钥；诊断仅输出固定白名单阶段和退出码，不在 Actions 日志中公开命令、服务器地址、域名、密码或业务凭据
- 上述实机门禁验证的是安装、升级、恢复、卸载和管理数据一致性，不等同于对真实客户端线路速度、运营商质量或长期业务流量的承诺

## 7.1.0 - 正式版：可恢复热更新、体检迁移、安全告警与 Naive H3（2026-08-27）

### 持久事务式热更新
- 更新流程升级为持久事务状态机：预检、运行快照、准备、原子切换、数据库迁移、健康门禁、提交或回滚均记录在 `/var/lib/rr-update/transactions`
- 写入服务在可恢复事务指针落盘后才会冻结；Nginx、Cloudflared、RR 防火墙规则及服务启停状态纳入版本化外部快照，恢复后再次核验，不完整时保持冻结并保留证据
- 新增开机早期恢复服务和独立 `rr-update-recover`，更新进程被杀、SSH 中断或系统重启后会先识别事务阶段，再恢复旧运行目录、配置、数据库及服务启停状态
- bundle、安装核心和更新守卫分别使用固定 SHA256 校验；运行目录切换前完成完整性、版本、防降级与 Python / JavaScript / Bash 预检
- 更新后检查 Sing-box 配置与进程、Nexus 数据库 quick_check、Nexus `/healthz`、订阅进程和原服务状态；任一门禁失败触发自动回滚尝试，恢复不完整时保留事务证据并要求执行恢复命令
- 保留最近一次已提交事务，支持 `rr update --rollback`；新增 Stable / Beta 通道和 `rr update --check`，发布构建可确定性复现并验证所有固定摘要
- CI 故障注入覆盖旧运行目录已移动、新运行目录已切换和数据库迁移阶段；Debian 12、Ubuntu 22.04、Ubuntu 24.04 容器矩阵执行安装、更新、中断恢复、手动回滚及卸载闭环

### 一键体检、加密备份与跨机迁移
- 新增 `rr doctor`，检查系统、DNS、时间、公网 IPv4/IPv6、Sing-box 配置与端口、防火墙、证书、Argo、Nexus、订阅、数据库、磁盘和更新源
- `rr doctor --repair` 只执行服务重载、目录权限等白名单安全修复；`--report` 输出自动遮蔽 UUID、IP、Token、域名和密钥的报告，`--json` 便于自动化接入
- 新增 `rr backup` / `rr restore`：`.rrbak` 使用 scrypt 派生密钥与 AES-256-GCM 认证加密，内含校验清单、SQLite 一致性备份、配置、设备、额度、流量周期、协议、Reality、Argo、Nexus、自动优选和 RR 定时任务
- 恢复前验证文件成员、摘要、数据库和系统环境；只覆盖 RR 管理范围并保留其他 crontab 项，恢复后健康检查失败会自动还原新服务器原状态

### RR Nexus 安全、告警与管理效率
- 新增 TOTP 两步验证、一次性恢复码登录和 WebAuthn Passkey；严格校验 RP ID、Origin、挑战、签名算法及签名计数器
- 新增 Telegram / HTTPS Webhook 告警中心，覆盖服务离线、磁盘、服务器套餐、证书、设备额度、Argo 临时域名变化、更新/备份失败和安全锁定，支持事件去重、敏感字段遮蔽及 Webhook HMAC 签名
- 新增 CPU / 内存 / 磁盘和上下行 24 小时、7 天、30 天历史趋势；样本自动聚合与清理
- 新增设备分组、配置模板、筛选和批量启停、移组、套模板、重置、删除；修复旧缓存前端选择模板后被默认“0 GB”覆盖的问题
- 将 TOTP / Passkey、通知和备份加密拆入独立库模块；SQLite 连接改为确定提交、回滚与关闭，降低长时间运行的文件描述符占用

### NaiveProxy HTTP/3 与安装完整性
- NaiveProxy 支持 HTTP/2、HTTP/3/QUIC 或双模式，新增 BBR / CUBIC / New Reno 拥塞控制和 `naive+quic://` 分享链接
- 旧配置升级默认保持 HTTP/2，新安装默认双模式；按模式分别开放 TCP / UDP，订阅、二维码、设备配置、Nexus 和远程面板同步识别 H2/H3
- 修复首次安装 Naive 时证书申请顺序、共享端口 TCP/UDP 校验，以及新装多协议未完整写入防火墙规则的问题

### 验证
- 新增 TOTP RFC 向量、WebAuthn ES256、Origin/计数器、CBOR 严格解析、AES-GCM 篡改拒绝与原子输出、Webhook HMAC、7.0.3 数据库无损迁移测试
- 完成 Nexus 登录、分组模板、模板回填、设备创建、批量停用/启用、告警保存、TOTP 初始化和 30 天历史范围浏览器实测

## 7.0.3 - 正式版：个人订阅、周期配额与服务器流量管理（2026-08-26）

### 个人订阅与客户端兼容
- mihomo、Clash Verge Rev、FlClash 拆分为各自独立的个人订阅地址与二维码；旧 Clash 与 Reality 单节点地址继续兼容热更新
- Sing-box 官方入口升级为完整多协议远程配置，选择器与自动测速组补齐 NaiveProxy 和 Argo 优选副节点
- 修复 mihomo YAML 将 TCP Keep Alive 写进代理节点、策略组使用无效 `default` 字段导致节点不通的问题；心跳改为顶层字段，默认节点改用 `default-selected`
- 所有个人订阅统一返回标准 `Subscription-Userinfo`、更新间隔与到期时间；主订阅静态服务升级为 RR 专用响应服务，使本地、公网 IP、公网域名三种模式均可动态显示上传、下载、剩余流量和到期时间
- 九种个人订阅在刷新时动态把“已用｜剩余｜到期”作为首个信息项；NekoBox、v2rayN、v2rayNG、Shadowrocket 与通用 URI 使用明确标注“勿选”的本机回环标记，确保 NekoBox 开启去重后仍保留且不会把流量发往远端，Sing-box 与 Clash 系列使用真实节点副本，源订阅文件保持不变
- 修复热更新只替换订阅服务文件、旧常驻进程却继续返回旧正文的问题；订阅服务状态加入程序哈希，文件变化后自动安全重启，并统一以设备 `used_bytes` 生成响应头和首位信息项
- 删除设备或令牌失效后订阅继续严格返回 404；设备停用、到期或额度用尽时返回带流量信息的空订阅，客户端仍可更新套餐状态但不能继续使用节点

### 设备配额与生命周期
- 个人设备新增自然月自动重置：可设置首次日期与续期次数，重置时保持原额度，月末日期按锚点计算防止逐月漂移
- 自动重置期间额度用尽即停用，到期日自动清零并恢复；最后一次续期结束后按计算出的最终日期到期
- 额度用尽后未发生自动或手动重置的设备，自动删除宽限期由 15 天延长为 35 天
- 设备备注新增本地与远程修改入口；备注仅保存在管理数据库，客户端节点名使用稳定随机别名，修改备注不重启 Sing-box、也不改变订阅地址与节点名

### 服务器套餐与远程管理
- RR Nexus 新增服务器流量套餐：可填写运营商总额度，并可在任意时间按运营商账单校准当前已用量；校准后重新建立网卡基线，再继续统计双向、仅 TX 或仅 RX 流量
- 网卡计数保存基线与累计差值，兼容面板重启、系统重启、网卡计数回退和自动选择默认出口；该功能只负责监控与预警，不会因运营商口径差异自动停止整台 VPS
- 远程副面板同步支持服务器套餐、设备周期重置、设备备注与完整个人订阅管理
- 本地面板 SSH 隧道加入 30 秒主动保活、6 次容错及端口转发失败快速退出，降低 NAT 或家宽长时间空闲造成的掉线概率

### 验证与发布
- 兼容旧数据库无损迁移；覆盖自然月末计算、自动重置恢复、35 天清理、真实网卡累计、重启与计数回退、动态订阅头和失效令牌拒绝测试
- 21 个发布清单文件、22 个 bundle 成员、6 种节点 URI、9 种个人订阅入口、前端契约与 Linux GitHub Actions 纳入统一回归

## 7.0.2 - 正式版：浏览器 Edge 初筛与更新链路加固（2026-08-24）

### 新增
- RR Nexus 新增 Cloudflare Edge 浏览器候选初筛：内置 1000 域名池，分层筛选 Stage 1 → Stage 2 → TOP 20
- 移动网络严格以 `openai.com` 为质量基准；Wi-Fi / 宽带使用 `openai.com`、`deepl.com`、`cloudflare.com` 三项参考门禁
- 支持均衡模式、亚洲入口狩猎、IPv4 / IPv6 / 双栈分组、成功率、TTFB、轮次波动、POP 与本机历史记录
- 面板初筛全部在用户浏览器执行，RR-vps 服务器不参与测速计算，也不接收结果

### 产品定位
- **面板优选质量不能只看排名**：排名只是浏览器候选顺序，不代表真实代理质量，也不保证第一名或前三名就是实际最快入口
- 用户必须把 TOP 20 放进真实客户端逐个测试，最终以实际速度、稳定性和晚高峰表现为准
- 面板版服务于没有原生测试环境的用户；Android **CF 域名优选 2.7.1** 可用于移动、电信、联通三网候选筛选，结果仍须在当前运营商、时段和真实客户端中复测
- Android 2.7.1 本版未改动

### 修复与加固
- 修复 Stage 1 成功轮次未计入，三轮全成功候选被错误显示为 67% 的问题
- 亚洲入口狩猎从仅有界面选项改为真实配额筛选与 HKG > NRT > SIN > ICN > TPE 排序
- 失败轮次按 0 纳入波动；VPN、出口变化或基准门禁失败时不保存为历史最佳
- 移动网络、宽带、运营商和协议的历史结果相互隔离，避免跨环境污染
- 更新器增加用户镜像 → GitHub Raw → GitHub 官方 Contents API → IPv4 CDN 多源回退，并区分下载链路失败与远程清单无效
- CI 正式执行仓库完整回归套件，覆盖 Bash 语法、发布清单、SHA256、Nexus 与更新链路契约
- 脚本终端为 VMess、VLESS Reality、Hysteria2、TUIC、AnyTLS、NaiveProxy 全部节点补齐二维码，并统一使用 M 级纠错与标准静区
- RR Nexus 修复公网 IP、IPv6、NAT/LXD 映射端口的订阅二维码地址；只展示真实生成的订阅格式，缺失格式不再产生无效二维码或错误回退
- 本地与远程面板二维码统一校验目标 URL、PNG 输出和异常超时，节点与订阅二维码使用同一条受检生成链

### 验证
- 1000 域名浏览器完整流程、移动网络 OpenAI 单基准、宽带三基准和 390px 手机布局通过
- 20 个发布清单文件、21 个 bundle 成员、6 种节点 URI、8 种面板订阅格式、前端单元检查与 Linux GitHub Actions 全部通过

## 7.0.1 - 正式版：Nexus 面板修复与远程设备管理闭环

### 修复
- 设备在线统计：新增「实时在线」口径（近 5 分钟有流量且启用），卡片显示「X 实时在线 · Y 启用」
- 防火墙端口状态：节点协议已监听端口即显示开启（原仅看防火墙规则，节点无独立规则时误报关闭）
- 分流下拉框白字：深色背景适配
- 分流应用后即时刷新状态；右上角刷新按钮覆盖防火墙/服务器/多服务器视图
- GPT 解锁检测：改用 cdn-cgi/trace 出口判定，数据中心 IP 不再误判地区封锁
- Cloudflare 隧道状态：快速隧道模式增加进程检测兜底
- CPU 参数固定宽度，大数字不再挤压布局
- 服务器卡片误显示「未同步」：修复状态合并逻辑
- 远程按钮失效与进度条满死：CSP 内联样式拦截修复（data-w + CSSOM）
- 远程设备 enabled 判断兼容 JSON 布尔
- 远程设备卡片对齐本地：绿灯状态 + 二维码透传
- 设备操作（添加/重置/修改/删除）同步后台化：节点重启不再误报断连
- 设备重名校验 + 错误提示中文化

## 6.6.18 - 订阅二维码改「打开二维码」按钮（与单节点一致）

### 调整
- 订阅地址列表二维码交互对齐单节点：每项右侧「打开二维码」按钮，
  点击在新窗口打开大图（window.open），扫码更稳
- 移除上一版点击放大弹窗方案（用户反馈按钮方式更方便）

## 6.6.17 - 订阅二维码支持点击放大

### 新增
- 订阅地址列表每项的二维码支持点击放大：弹出居中大图（320px 锐利渲染），
  方便扫描添加设备；点击任意位置关闭
- 前端 modal 纯 JS 动态创建，不新增依赖

## 6.6.16 - 面板订阅地址列表每项都带二维码

### 新增
- 设备详情页的订阅地址列表（总订阅/NekoBox/URI 全集/Sing-box 完整）此前只有
  第一项有二维码；现在每项右侧都有二维码，扫码即得订阅，方便快速添加设备
- 后端 /api/devices/{id}/qr 新增 raw 参数（仅接受 http/https），前端按 URL 逐项生成

## 6.6.15 - 修复 LE 证书 403（root umask 077 权限链）

### 修复
- DMIT 等模板 root umask 077：mkdir 出 700 目录、certbot 写 600 文件，
  nginx(www-data) 读挑战文件 Permission denied → LE 验证 403（五哥实机复现）
- 面板域名模式与 NaiveProxy 的 webroot 流程：显式 chmod 755 目录链
  （webroot/.well-known/acme-challenge）+ umask 022 子壳跑 certbot（文件 644）
- 实机对比：Vultr（umask 022）一次过，DMIT（umask 077）403——现已强制权限

## 6.6.14 - 修复面板域名模式 LE 证书 404（nginx 未 reload）

### 修复
- nexus_write_nginx_site / nexus_write_nginx_custom_port 写完 server 块只做 nginx -t
  不 reload——nginx 继续跑 default 站，acme-challenge 请求 404，Let's Encrypt 签发失败
- 现在语法检查通过后立即 reload（reload 失败退级 restart），新配置即时生效

## 6.6.13 - 修复面板安装 typing_extensions 冲突（6.6.10 回归）

### 修复
- 6.6.10 起 grpcio 统一 pip 安装，在 Ubuntu 24.04 上 pip 升级 apt 版 typing_extensions
  时报 "Cannot uninstall typing_extensions 4.10.0, RECORD file not found" 导致面板装不上
- 新策略：已装新版跳过 → apt 源版优先（Debian12=1.51/Ubuntu24=1.60，无 pip 冲突）
  → 仅 Ubuntu22（源版 1.41）走 pip 兜底，兜底前先 --ignore-installed 装 pip 版
  typing_extensions shadow 掉 debian 版（/usr/local/lib 优先）
- 实机验证：CS2 Ubuntu22 卸净重装走 pip 兜底 1.83 面板 active；CS3 半删残留异常态
  亦兜底成功；面板服务不受影响

## 6.6.12 - 订阅服务自愈链加固

### 修复
- 健康检查自愈订阅时 `generate_node_and_sub` 无超时保护：重建含公网入口探测，网络抖动会把整个健康检查链卡住，导致订阅服务迟迟未拉起。现在加 `timeout 90` 兜底，卡住也不阻断后续检查

### 验证
- CS1 实测：杀订阅服务 → `rr --health-check` 一次拉起（3.8s）；timer 每 5 分钟自动巡检正常
- 前链实测（6.6.11）：H1 截断 bundle 回滚 / H2 乱码 manifest 拒绝 / B5 default→singbox_node 全过

## 6.6.11 - P2 修复：防火墙关闭拒绝基线 + 设备同步 O(n) 合并

### 修复（P2 ×2）
- **防火墙 close 无拒绝基线**：INPUT 默认策略 ACCEPT 时，删除放行规则后端口依然可达。现在 `close_protocol_firewall` 在链尾补一条 `DROP`（comment: argo-rr-managed-block），关闭=明确拒绝；`open_protocol_firewall` 先删该 DROP 再放行，语义闭环（iptables/ip6tables/ufw 三路一致，不影响其他服务）
- **设备操作 O(n)**：批量设备变更（如批量删除）触发 N 次全量重建（每次 13-53s），面板被拖到 504。`sync_devices` 加合并窗口：同步进行中时新请求标记 pending 秒回，执行完 drain 兜住期间全部变更——批量 20 并发实测从 20 次重建合并为 2 次

### 验证
- 防火墙（CS1 实机）：open=HTTP 200 连通 → close=DROP 在位+连接超时拒绝 → re-open=HTTP 200 恢复；规则无残留
- debounce（本地并发单测）：20 并发调用全部 True、实际执行 2 次（首+尾 drain）
- 面板 smoke：CS1 加载新 rr_nexus.py 重启后 active、HTTP 200

## 6.6.10 - P1 修复：grpcio 旧版面板 CPU 死循环

### 修复（P1）
- grpcio < 1.43 在 Ubuntu 22.04 上会导致 Nexus 面板 CPU 死循环（apt 源 python3-grpcio 为 1.41，过旧）
- `nexus_install_dependencies` 改用 pip 统一安装 `grpcio>=1.43`（兼容 PEP 668：Debian 12 / Ubuntu 24.04 自动加 `--break-system-packages`），并卸载 apt 版避免并存冲突，装后强制校验版本
- 幂等：已装新版时 pip 自带版本比较直接跳过；更新流程面板重启后自动加载新版

### 验证
- 三系统实机（Debian 12 / Ubuntu 22.04 / Ubuntu 24.04）：依赖函数 FN_RC=0、grpcio 1.83.0、面板重启后 3 分钟观察 CPU 0.2%（无死循环）

## 6.6.9 - 稳定基线（系统级审计修复版，全量实机验证 103 PASS/0 FAIL）

### 修复（7 个产品缺陷全闭环）
- 到期设备不清扫 + 订阅服务与面板 /sub 双路由语义不一致 → 幂等清扫标记 + SQL 过滤对齐
- toggle 失败伪装成功（403 变 200 {}）→ 非零退出也解析 stdout 透传真实状态
- 无公网证书时 revoke 会重新生成 key → 与签发对称补证书门槛
- Nexus 数据库损坏无限崩溃循环 → quick_check 完整性检查 + 退出码 3 + StartLimit（不自动重建，数据安全优先）
- Naive LE 证书邮箱 admin@example.com 被 LE 恒拒 → 未配置时从域名派生，LE_EMAIL 可配置
- /nexus/ 目录泄露设备 token → index.html 防护
- cron worker PATH 缺 /usr/local/bin → 孤儿清理失效修复

### 增强（完善项 + 可靠性改进）
- TLS 证书删除后自动重建，节点不再因证书丢失无法启动
- NAIVE 未启用时清空遗留掩码凭据，保持配置终态干净
- 迁移后活节点机器健康定时器介入，判定收紧
- 直连自签模式补防火墙规则
- SNI map 修复（443 节点流量精确归属面板）
- 审计补全两路径（成败双落账 + revoke denied）
- 断网时更新检查三态（有新/已最新/检查失败），不再误报"已是最新"
- 内核自愈失败写日志 + 面板提示（不再静默吞错）
- 孤立 sing-box 进程三通道提示（仅提示不清理）
- 降级拦截显式提示
- 升级失败自动回滚全链路（坏 bundle/坏 config/降级不兼容）
- 旧版配置迁移后热更升级字段零丢失（兼容性实测通过）
- Debian 12 / Ubuntu 22.04 / Ubuntu 24.04 全新安装实测通过
