"""Shared RR Nexus runtime helpers.

The package keeps security, notification and backup primitives out of the HTTP
router.  Every module remains dependency-light and is shipped in the verified
RR release bundle.
"""
